aliases:
  - default
  - clean:dev
  - connect
  - watch
clean:
  - '<%= devDist %>'
connect:
  options:
    livereload: true
    base: '<%= devDist %>'
watch:
  files:
    - '<%= devDist %>/**/*'
  options:
    livereload: true
